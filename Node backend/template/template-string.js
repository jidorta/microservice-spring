const deadpool ={
    nombre ='Wade',
    apellido = 'Winston',
    poder = 'generacion',
    getNombre (){
        return `${this.nombre} ${this.apellido}`

    }
}

console.log(deadpool.getNombre())
;